import { Routes } from '@angular/router';
import { MonitoringComponent } from '../features/monitoring/pages/monitoring';

export const routes: Routes = [
  {
    path: '',
    component: MonitoringComponent,
  },
];
